Analyze external dictionaries.

Use the `altinity-expert-clickhouse-dictionaries` skill instructions in:
`../skills/altinity-expert-clickhouse-dictionaries/SKILL.md`

Connect using clickhouse-client with CLICKHOUSE_* env vars (use --secure if CLICKHOUSE_SECURE=1).

Focus on the `altinity-expert-clickhouse-dictionaries` database. Report dictionary status, memory usage (bytes_allocated) for `user_dict`, and total dictionary memory as % of RAM. Include a clear “Dictionary Overview” section.

Include severity ratings for key findings.
