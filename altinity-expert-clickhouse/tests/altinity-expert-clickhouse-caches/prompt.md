Analyze ClickHouse cache health.

Use the `altinity-expert-clickhouse-caches` skill instructions in:
`../skills/altinity-expert-clickhouse-caches/SKILL.md`

Focus on the `altinity-expert-clickhouse-caches` database and report:
- Mark cache size and hit ratio
- Uncompressed cache size and hit ratio (if available)
- Query cache summary (if available)
- Tables contributing most to marks

Produce a markdown report with severity ratings and recommendations.
