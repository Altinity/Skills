Analyze table schema and design.

Use the `altinity-expert-clickhouse-schema` skill instructions in:
`../skills/altinity-expert-clickhouse-schema/SKILL.md`

Focus on the `altinity-expert-clickhouse-schema` database and report partitioning, ORDER BY, nullable columns, and long names issues.

Include a "Schema Overview" section and severity ratings. Explicitly report partition severity for `schema_daily_partitions`.
