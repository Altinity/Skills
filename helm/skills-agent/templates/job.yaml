{{- if .Values.debugMode }}
apiVersion: v1
kind: Pod
metadata:
  name: {{ include "skills-agent.fullname" . }}-debug
  labels:
    {{- include "skills-agent.labels" . | nindent 4 }}
    skills-agent/skill: {{ .Values.skillName | quote }}
    skills-agent/agent: {{ .Values.agent | quote }}
    skills-agent/mode: "debug"
spec:
  restartPolicy: Never
  {{- include "skills-agent.podSpecBase" . | nindent 2 }}
{{- else }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "skills-agent.fullname" . }}-{{ now | date "20060102-150405" }}
  labels:
    {{- include "skills-agent.labels" . | nindent 4 }}
    skills-agent/skill: {{ .Values.skillName | quote }}
    skills-agent/agent: {{ .Values.agent | quote }}
spec:
  {{- if .Values.job.ttlSecondsAfterFinished }}
  ttlSecondsAfterFinished: {{ .Values.job.ttlSecondsAfterFinished }}
  {{- end }}
  {{- if .Values.job.activeDeadlineSeconds }}
  activeDeadlineSeconds: {{ .Values.job.activeDeadlineSeconds }}
  {{- end }}
  backoffLimit: {{ .Values.job.backoffLimit }}
  template:
    metadata:
      labels:
        {{- include "skills-agent.selectorLabels" . | nindent 8 }}
        skills-agent/skill: {{ .Values.skillName | quote }}
        skills-agent/agent: {{ .Values.agent | quote }}
    spec:
      restartPolicy: {{ .Values.job.restartPolicy }}
      {{- include "skills-agent.podSpecBase" . | nindent 6 }}
{{- end }}
